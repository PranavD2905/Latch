# Must match AUDIT_TRAIL_TOKEN in the repo root's .env — the SSE server
# (src/adapters/audit-trail/) checks it as a query param, since EventSource
# cannot send custom headers.
VITE_AUDIT_TRAIL_TOKEN=
