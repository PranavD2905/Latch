DATABASE_URL=postgres://latch:latch@localhost:5432/latch

# Razorpay test-mode keys. Required for the RazorpayPaymentProvider live
# integration test (src/adapters/payment/razorpay-payment-provider.live.integration.test.ts)
# and for `npm run mcp:dev:razorpay`. Get these from Dashboard > Test Mode > API Keys.
# Never commit real values here.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Static merchant token for the merchant-only API (src/adapters/merchant-api/).
# The one thing an agent can never present — decline_booking sits behind this,
# not behind the MCP tool surface. Any non-empty string works for local dev.
MERCHANT_API_TOKEN=

# Simple token for the Slice 6 audit-trail SSE viewer (src/adapters/audit-trail/).
# Sent as a query param (?token=...), not a header — EventSource can't set
# custom headers. Any non-empty string works for local dev.
AUDIT_TRAIL_TOKEN=
